package com.anmi.devworkspace.library.search

/** Supported presentation orderings for library search results. */
enum class LibrarySort {
    DEFAULT,
    TITLE,
    CREATED_AT,
    UPDATED_AT,
    TYPE,
}
